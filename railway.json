{
  "$schema": "https://railway.app/railway.schema.json",
  "$comment": "For Docker deployments, Railway automatically detects Dockerfile. No startCommand needed - Docker CMD handles it.",
  "deploy": {
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
